// please implement it

#include "Graph.hpp"
Graph::Graph(int size)
{
	verticies.reserve(size);
}

Graph::buildGraph()
int foo;
ifstream myfile ("example.txt");

while (myfile >> foo)
{
	if (foo == -1)
	{
		
	}
}

else cout << "Unable to open file"; 

  return 0;
}